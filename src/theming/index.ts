
const appColors = {
    background: "var(--colors-background)",
    surface: "var(--colors-surface)",
    onBackground: "var(--colors-on-background)",
    onSurface: "var(--colors-on-surface)",
    primary: "var(--colors-primary)"
};

const appTheme = {
    colors: appColors,
    fonts: {
        heading: "var(--fonts-heading)",
        body: "var(--fonts-body)"
    }
}

export default appTheme;